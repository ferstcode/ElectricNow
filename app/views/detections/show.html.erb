<div class="card text-center">
  <div class="card-header">
    <p>Cliente: <%= @detection.user.name %></p>         
  </div>
  <div class="card-body">
    <%= image_tag @detection.image, height: '200', alt:"Imagen del hallazgo"%>                  
    <p class="card-text">Detalle del hallazgo: <%=@detection.detail %></p>
    <p class="card-text">Dirección: <%=@detection.address %> en la comuna: <%= @detection.commune %></p>
    <div id="map" style= "width: 300px; height: 300px;">

    </div>
    <p class="card-text">Disponibilidad para recibir visita del eléctrico: <%=@hora%> hrs. el día <%=@dia %></p> 
  </div>
  <div class="card-footer text-muted">  
    <%=link_to 'Realizar cotización', new_detection_quatation_path(@detection.id), remote: true%>
    <%=link_to 'Volver', user_profile_path(current_user), class:"btn btn-primary"%>       
  </div>
</div>

<script>
      handler = Gmaps.build('Google');

  $(document).ready(function(){
    handler.buildMap({ provider: {}, internal: {id: 'map'}}, function(){
      markers = handler.addMarkers([
        {
          "lat": <%= @detection.latitude %>,
          "lng": <%= @detection.longitude %>,
          "picture": {
          "url": "http://people.mozilla.com/~faaborg/files/shiretoko/firefoxIcon/firefox-32.png",
          "width":  32,
          "height": 32
        },
        "infowindow": "hello!"
        }
      ]);      
      handler.bounds.extendWith(markers);
      handler.fitMapToBounds();
      center.setMap(map);
    });
    markers = handler.addMarkers(<%=raw @hash.to_json %>);
  });
</script>







