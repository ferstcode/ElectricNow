<div class="jumbotron">
    <div class="container text-center">
        <div class="row">
            <div class="col-sm"></div>
            <div class="col-sm">
              <%= image_tag current_user.image, size: '150', class:'rounded-circle', alt: 'Foto Usuario' %>
            </div>
            <div class="col-sm">
                <h2 class="text-center"><%= current_user.name %></h2>               
            </div>
        </div>
    </div>
</div>
<div class="container">  
  <%=link_to 'Volver', :back, class: 'btn btn-outline-secondary btn-lg'%>
</div>

