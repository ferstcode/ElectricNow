<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#">
        <%= image_tag 'logo.svg', class:'logo d-inline-block align-top', alt:'Logo de Empresa' %>
        ElectricNow
    </a>    
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>          
    <div class="collapse navbar-collapse " id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <% if user_signed_in? %>
                
                <% if can? :create, Quatation %>
                        <li> <%= link_to 'Mis Cotizaciones', user_quatations_path, class: 'nav-link'%></li>
                <% end %>
                <% if can? :create, Detection %>
                    <li><%=link_to 'Publicar hallazgo', new_detection_path, remote: true, class:'nav-link'%></li>            
                <% end %>
                <% if can? :read, User %>
                    <li><%=link_to 'Eléctricos', electrics_path, class:'nav-link'%></li>            
                <% end %>
                <% if current_user.electric? %>
                    <% if can? :read, Detection%>
                        <li> <%= link_to 'Detecciones recibidas', detections_electric_path(current_user.id), class: 'nav-link' %> </li>            
                    <% end  %>
                <% end %>
            <% end %>                    
        </ul>
        <ul class="navbar-nav navbar-rigth">
            <% if user_signed_in? %>
                <li class="nav-item">                    
                    <span> <%= image_tag current_user.image, size: '40', class:'rounded-circle'%> <%= current_user.name %> </span>
                </li>               
                <li><%= link_to 'Cerrar sesión', destroy_user_session_path, method: :delete, class: 'nav-link' %></li>
            <% else %>
                <li><%= link_to 'Regístrate', new_user_registration_path, class: 'nav-link'  %></li>      
                <li><%= link_to 'Inicia sesión', new_user_session_path, class: 'btn btn-outline-primary' %></li>
            <% end %>          

        </ul>
        
    </div>
</nav>